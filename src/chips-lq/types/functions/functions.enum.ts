export enum Functions {
    // AGGREGATE
    MAX = 'max',
    MIN = 'min',
    COUNT = 'count',

    // SCALAR
    LOWER = 'lower',
    UPPER = 'upper',
    CONCAT = 'concat',

    // Conditionals
    IF = 'if',
    COALESCE = 'coalesce',

    // Casting
    CAST = 'cast',

    // CUSTOM
    CUSTOM = 'custom'
}