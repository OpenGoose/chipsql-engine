export enum MssqlDataTypes {
    INT = 'int',
    DECIMAL = 'decimal',
    BIGINT = 'bigint',
    TINYINT = 'tinyint',
    
    VARCHAR = 'varchar',
    BIT = 'bit',

    DATETIME2 = 'datetime2',
    DATE = 'date',
}