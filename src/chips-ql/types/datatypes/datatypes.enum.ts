export enum DataType {
    // Text
    VARCHAR = 'varchar',

    // Numeric
    BYTE = 'byte',
    INT = 'int',
    BIGINT = 'bigint',
    DECIMAL = 'decimal',

    // Bool
    BOOLEAN = 'bool',

    // Time
    DATE = 'date',

    // Custom
    CUSTOM = 'CUSTOM',
}