export enum DataType {
    // Text
    STRING = 'string',

    // Numeric
    NUMBER = 'number',

    // Bool
    BOOLEAN = 'bool',

    // Time
    DATE = 'date',

    // Custom
    CUSTOM = 'CUSTOM',
}